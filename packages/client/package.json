{
  "name": "@tamtri/client",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "DaemonClient SDK used by every surface: WebSocket transport, request/response correlation, event subscriptions, and relay E2EE wrapping.",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "node --import tsx --test test/*.test.ts"
  },
  "dependencies": {
    "@tamtri/protocol": "workspace:*",
    "@tamtri/relay": "workspace:*",
    "base64-js": "^1.5.1"
  },
  "devDependencies": {
    "@types/node": "^26.1.1",
    "tsx": "^4.23.0",
    "typescript": "^5.8.3"
  }
}
