// Placeholder Expo Router root layout. Replaced in the app build-out step with
// the real providers (unistyles theme, react-query, host runtime, gesture root).
export default function RootLayout(): null {
  return null;
}
